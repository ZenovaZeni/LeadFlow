# Visual Audit Rule

Whenever a task involves **visual design**, **layout balance**, **responsiveness**, or **CSS styling**, you **MUST** run a browser subagent/Playwright audit of the page *before* and *after* making changes to verify the visual impact. Do not rely solely on code inspection for spacing, density, or sizing.
